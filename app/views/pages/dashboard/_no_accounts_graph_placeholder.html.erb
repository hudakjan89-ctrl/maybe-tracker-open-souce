<div class="h-[300px] lg:h-[340px] bg-container shadow-border-xs rounded-xl py-4 flex flex-col items-center justify-center">
  <div class="space-y-3 text-center flex flex-col items-center">
    <%= render DS::FilledIcon.new(
      variant: :container,
      icon: "layers",
    ) %>

    <p class="text-sm font-medium text-primary">Vitajte vo vašom finančnom trackeri</p>
    <p class="text-secondary text-sm">Pridajte účet a potom importujte PDF výpis z Tatra banky</p>
    <div class="flex flex-col sm:flex-row gap-2 items-center">
      <%= render DS::Link.new(
      text: "Pridať účet",
      icon: "plus",
      href: new_account_path,
      frame: :modal
    ) %>
      <%= render DS::Link.new(
      text: "Výpis Tatra banky",
      icon: "download",
      href: new_tatra_statement_path,
      frame: :modal,
      variant: "outline"
    ) %>
    </div>
  </div>
</div>
