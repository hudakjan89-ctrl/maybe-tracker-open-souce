<% content_for :title, "Chýba nastavenie Redisu – Maybe" %>

<div class="flex items-center justify-center h-full p-4 sm:p-6 lg:p-8">
  <div class="w-full max-w-md sm:max-w-lg lg:max-w-2xl">
    <div class="bg-container border border-primary rounded-xl p-6 sm:p-8 shadow-sm">
      <!-- Icon and Header -->
      <div class="text-center mb-8">
        <div class="mx-auto w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mb-4">
          <%= icon "alert-triangle", class: "w-8 h-8 text-red-600" %>
        </div>
        <h1 class="text-xl sm:text-2xl font-bold text-primary mb-2">Je potrebné nastaviť Redis</h1>
        <p class="text-sm sm:text-base text-muted">Vaša inštalácia Maybe potrebuje správne nastavený Redis.</p>
      </div>

      <!-- Explanation -->
      <div class="mb-8">
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
          <div class="flex items-start">
            <%= icon "info", class: "w-5 h-5 text-amber-600 mt-0.5 mr-3 flex-shrink-0" %>
            <div class="text-sm text-amber-800">
              <p><strong>Prečo je Redis potrebný?</strong></p>
              <p class="mt-1">Maybe používa Redis na spúšťanie úloh na pozadí (Sidekiq) — synchronizáciu účtov, spracovanie importov a ďalšie operácie, ktoré udržiavajú vaše finančné údaje aktuálne.</p>
            </div>
          </div>
        </div>

        <!-- Primary CTA -->
        <div class="text-center space-y-4">
          <%= render DS::Link.new(
            text: "Zobraziť návod na nastavenie",
            href: "https://github.com/maybe-finance/maybe/blob/main/docs/hosting/docker.md",
            variant: "primary",
            size: "lg",
            icon: "external-link",
            full_width: true,
            target: "_blank",
            rel: "noopener noreferrer"
          ) %>
          <p class="text-sm text-muted">Postupujte podľa nášho návodu na nastavenie Dockera a nakonfigurujte Redis</p>
        </div>
      </div>

      <!-- Secondary CTA -->
      <div class="pt-6 border-t border-primary">
        <div class="text-center space-y-3">
          <p class="text-sm text-muted">Po nastavení Redisu obnovte túto stránku.</p>
          <%= render DS::Button.new(
            text: "Obnoviť stránku",
            variant: "secondary",
            icon: "refresh-cw",
            type: "button",
            full_width: true,
            onclick: "window.location.reload()"
          ) %>
        </div>
      </div>
    </div>
  </div>
</div>
