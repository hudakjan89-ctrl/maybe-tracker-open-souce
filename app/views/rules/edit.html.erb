<%= link_to "Späť na pravidlá", rules_path %>

<%= render DS::Dialog.new do |dialog| %>
  <%
    title = if @rule.name.present?
              "Upraviť pravidlo „#{@rule.name}“"
            else
              "Upraviť pravidlo"
            end
  %>
  <% dialog.with_header(title: title) %>

  <% dialog.with_body do %>
    <%= render "rules/form", rule: @rule %>
  <% end %>
<% end %>
