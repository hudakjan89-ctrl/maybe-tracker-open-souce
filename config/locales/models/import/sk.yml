---
sk:
  activerecord:
    attributes:
      import:
        currency: Mena
        number_format: Number Formaat
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: is not a valid CSV format
