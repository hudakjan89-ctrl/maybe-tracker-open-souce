---
sk:
  activerecord:
    attributes:
      import:
        currency: Mena
        number_format: Formát čísel
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: nie je platný formát CSV
